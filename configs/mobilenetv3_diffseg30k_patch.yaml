seed: 42
device: cuda

data:
  root: data/diffseg30k_patch
  image_size: 224
  num_workers: 8
  class_names: [real, fake]

train:
  epochs: 20
  batch_size: 64
  lr: 0.0003
  weight_decay: 0.0001
  label_smoothing: 0.05
  amp: true
  grad_clip_norm: 1.0
  early_stop_patience: 6

model:
  backbone: mobilenet_v3_small
  pretrained: true
  branch_mode: full
  dropout: 0.25
  hidden_dim: 512

output:
  dir: outputs_diffseg30k_patch
  checkpoint_name: best_model.pt
